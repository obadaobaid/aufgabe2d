using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class boilerCode : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //1 deteremain the frequency of the screen and calculate the disappearing time
        //( float refreshRate = Screen.currentResolution.refreshRate;
        //Debug.Log("Screen refresh rate: " + refreshRate + " Hz");)
        //2 create a copy of the CSV file and open it
        //3 add new column called response if not exist
        
        //for each trial
            //4 calculate the disappearing pattern from the file
            //5 decide if is_probe left of right
            //6 view the basic scene for a random time (300,500 millisecond)
            //7 the first box disappear for the calculated time in 1
            //8 the second box disappear for the calculated time in 1

            //read the user answer from animation (collider)
            //write the answer in the same raw under the new column created in 3
        
            //the square disappear







    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
